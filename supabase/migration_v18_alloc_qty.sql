-- ================================================================
-- Migration v18: 부킹 도착지별 배분수량 (alloc_qty)
--   같은 부킹번호를 여러 도착지가 공유할 때, 각 행(도착지)의
--   배분 대수를 저장한다. 화면에는 "배분/그룹총량"(예: 10/100)으로 표시.
-- Supabase SQL Editor에서 실행하세요. (재실행 안전)
-- ================================================================

ALTER TABLE bookings ADD COLUMN IF NOT EXISTS alloc_qty NUMERIC;
