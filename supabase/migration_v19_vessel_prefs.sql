-- ================================================================
-- Migration v19: 부킹장(모선) 개인 설정 (열 순서·표시 열)
--   계정 단위 저장 — 어느 컴퓨터에서 접속해도 유지된다.
-- Supabase SQL Editor에서 실행하세요. (재실행 안전)
-- ================================================================

ALTER TABLE profiles ADD COLUMN IF NOT EXISTS vessel_prefs JSONB;
